import styled from 'styled-components'

export const Wrapper = styled.div`
  img {
    object-fit: cover;
    margin-left: 20px;
  }

  p {
    font-weight: bold;
    word-wrap: break-word;
  }
  .table {
    minwidth: 650;
    margin-bottom: 15px;
  }

  .buttons {
    max-height: 15px;
  }
`
